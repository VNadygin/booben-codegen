import React from "react";
import { BrowserRouter, Route, Switch } from "react-router-dom";

const Router = props => <BrowserRouter><Switch><Route path="/" exact={false} component={[object Object]} /></Switch></BrowserRouter>;

export default Router;